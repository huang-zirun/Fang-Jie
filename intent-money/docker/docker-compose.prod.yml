services:
  backend:
    build:
      context: ../backend
    # 注意：env_file 加载后，environment 中的值会覆盖 env_file 中的值
    # 所以这里明确设置关键环境变量，确保它们生效
    env_file:
      - ../backend/.env
    environment:
      DATABASE_URL: sqlite+aiosqlite:////app/data/intent_money.db
      ENV: production
      DEV_MODE: "false"
      SCRAPER_ENABLED: "false"
      AUTO_PUBLISH_ENABLED: "false"
      SOCIAL_AUTO_UPLOAD_PATH: ""
      # Cookie 加密密钥（AES-256 需要 32 字节密钥的 Base64 编码）
      # 这个值会覆盖 .env 文件中的值
      COOKIE_ENCRYPTION_KEY: "vKLKKWQTD8InVdumaBSrdxk8KFZ8GmWWO4YyJaStMUA="
    volumes:
      - db_data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 60s

  nginx:
    build:
      context: ../frontend
    volumes:
      - ./nginx.prod.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "127.0.0.1:9090:80"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped

volumes:
  db_data:
